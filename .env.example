NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_DB_PASSWORD=

# Opcional: sin ella la ruta se genera igual, sin la personalización con IA. Los quizzes no la usan.
OPENAI_API_KEY=

# Opcional: la URL pública del sitio, sin barra final. Si está, la usan el login (redirectTo) y la
# tarjeta OG de /shared/[slug]; si no, la tarjeta usa el dominio de producción de Vercel o, en local,
# http://localhost:3000.
NEXT_PUBLIC_SITE_URL=
